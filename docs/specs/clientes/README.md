# Módulo Clientes — Specs

## Dependências entre specs:

- **Fase 1 (P0):** CLI-01 (campos faltantes — fundação)
- **Fase 2 (P1):** CLI-02 → CLI-03 → CLI-05 → CLI-06 → CLI-04 (pipeline → tags → filtros → busca → timeline)
- **Fase 3 (P2):** CLI-07, CLI-08, CLI-09, CLI-10 (independentes entre si)
- **Fase 4 (P3):** CLI-11, CLI-12 (independentes)

## Arquivos existentes no frontend:
- `Clientes.jsx` (6.3 KB) — listagem
- `ClienteForm.jsx` (14.3 KB) — formulário
- `ClienteDetalhe.jsx` (9 KB) — detalhe
